<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <TextView android:layout_height="wrap_content"  android:layout_width="fill_parent"  android:layout_marginLeft="10dp" android:id="@+id/login_user_text" android:text="@string/username" android:layout_marginTop="5dp"></TextView>
    <EditText android:layout_height="wrap_content" android:text="" android:layout_width="fill_parent" android:layout_marginLeft="10dp"   android:id="@+id/login_user_edittext" android:hint="@string/login_user_hint" android:layout_marginTop="5dp" android:background="@color/white" android:textColor="@color/black" android:cursorVisible="true" android:inputType="text|textNoSuggestions" android:textCursorDrawable="@null"></EditText>
    <TextView android:layout_height="wrap_content"  android:id="@+id/login_password_text" android:layout_width="fill_parent" android:text="@string/password" android:layout_marginTop="5dp"></TextView>

    <EditText
        android:id="@+id/login_password_edittext"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="5dp"
        android:background="@color/white"
        android:cursorVisible="true"
        android:inputType="text|textPassword"
        android:textColor="@color/black"
        android:textCursorDrawable="@null" >
</EditText>

    <Button
        android:id="@+id/login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:layout_marginRight="20dp"
        android:layout_marginTop="5dp"
        android:text="@string/login" >
</Button>

    <ListView
        android:id="@+id/user_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ListView>

</LinearLayout>
